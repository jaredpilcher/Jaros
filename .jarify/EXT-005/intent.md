# Intent

Every agent runs inside an unyielding harness that owns the rules. Agents hold no ambient power: they receive only explicitly granted, revocable capabilities, every side effect is a mediated and validated request, and nothing can bypass or rewrite the rules at runtime (fail closed by default). The harness — not the agents — governs the system, exactly as the Prime Directive requires.
