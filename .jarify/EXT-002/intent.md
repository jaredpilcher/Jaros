# Intent

Make system progress an explicit, durable, replicated sequence of validated state transitions — never application logic scattered across services. The machine must be unbreakable: it can never enter an undefined state, every committed transition survives a crash, and the loss of any single node loses nothing. This is the deterministic backbone the Prime Directive demands.
