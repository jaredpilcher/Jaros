{
  "name": "jaros",
  "version": "0.1.0",
  "description": "An unbreakable distributed state machine that orchestrates AI agents as lightweight threads.",
  "private": true,
  "license": "MIT",
  "main": "dist/src/main.js",
  "scripts": {
    "build": "tsc -p .",
    "checks": "npm run check:planes && npm run check:no-server && npm run check:comms",
    "check:planes": "node dist/scripts/check-planes.js",
    "check:no-server": "node dist/scripts/check-no-server.js",
    "check:comms": "node dist/scripts/check-comms.js",
    "test:unit": "node --test \"dist/**/*.test.js\"",
    "test": "npm run build && npm run checks && npm run test:unit",
    "test:integration": "node dist/test/integration/docker-run.js",
    "start": "node dist/src/main.js"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2"
  }
}
