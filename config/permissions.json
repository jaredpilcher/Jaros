{
  "roles": {
    "AnalystRole": {
      "description": "Read-only access to the shared file system.",
      "actions": [
        "fs.read"
      ]
    },
    "ReporterRole": {
      "description": "Write access to the shared file system and ability to send jobs to queues.",
      "actions": [
        "fs.write",
        "queue.send"
      ]
    },
    "AdminRole": {
      "description": "Full built-in capabilities: file system access and queue manipulation.",
      "actions": [
        "fs.read",
        "fs.write",
        "queue.send",
        "queue.receive"
      ]
    },
    "GuestRole": {
      "description": "Minimal role with only read access to files.",
      "actions": [
        "fs.read"
      ]
    }
  },
  "assignments": {
    "advance": "AdminRole",
    "custom_agent": "AnalystRole",
    "my_agent": "ReporterRole"
  }
}
