"""Bundled starter agents/tools/evals/schedules for `jaros init --with-examples`.

Shipped inside the package so the command works from a `pip install jaros`
(read via importlib.resources). Mirrors the read-only library + swarm hive under
the repo `examples/`.
"""
